# Creating a list
numbers = [1, 2, 3, 4, 5]

# Accessing elements
print(numbers[0])  # Output: 1

# Modifying elements
numbers[2] = 10

# Adding elements
numbers.append(6)

# Removing elements
numbers.remove(3)

# List length
length = len(numbers)

# Printing list
print(numbers, length)
